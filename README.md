# Heuk Sung Schedule (GitHub Pages)

## 사용 방법
1. 이 폴더를 GitHub 레포지토리에 업로드합니다.
2. `Settings -> Pages`에서 배포 브랜치(보통 `main`)와 루트(`/`)를 선택합니다.
3. 접속하면 기본 배경(`default-bg.jpg`) + 스케줄 양식이 보입니다.
4. 오른쪽 위 파일 업로드 버튼으로 배경 이미지를 즉시 교체할 수 있어요.

## 파일 구성
- `index.html` : 스케줄표 페이지
- `default-bg.jpg` : 기본 배경 이미지
